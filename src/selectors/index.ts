import { RootState } from '@/types'

export const userState = (state: RootState) => state.userState
export const reposState = (state: RootState) => state.reposState
export const repoState = (state: RootState) => state.repoState
export const contentsState = (state: RootState) => state.contentsState
